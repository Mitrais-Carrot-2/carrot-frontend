/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["images.unsplash.com", "st.depositphotos.com", "localhost"],
  },
};

module.exports = nextConfig;
